import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard",
  description: "Overview of fabric resources and recent activity.",
};

export default function Page() {
  return <div>Dashboard</div>;
}
